import 'package:flutter/material.dart';

class FeedbackTabScreen extends StatelessWidget {
  const FeedbackTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('FeedbackTabScreen')),
      body: Center(child: Text('TODO: FeedbackTabScreen')),
    );
  }
}
